import PlaceholderPage from '../ui/PlaceholderPage';
export default function AICreationWorkflow({ onComplete }: { onComplete?: () => void }) {
  return <PlaceholderPage title="AI Creation Workflow" description="Generate content and code with AI." />;
}