import PlaceholderPage from '../ui/PlaceholderPage';
export default function EnterpriseSecurityWorkflow({ onComplete }: { onComplete?: () => void }) {
  return <PlaceholderPage title="Enterprise Security" description="Configure enterprise-grade security." />;
}