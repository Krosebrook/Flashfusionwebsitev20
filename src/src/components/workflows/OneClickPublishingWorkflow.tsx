import PlaceholderPage from '../ui/PlaceholderPage';
export default function OneClickPublishingWorkflow({ onComplete }: { onComplete?: () => void }) {
  return <PlaceholderPage title="Publishing Workflow" description="Deploy your application to multiple platforms." />;
}