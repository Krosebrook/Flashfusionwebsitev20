import PlaceholderPage from '../ui/PlaceholderPage';
export default function CreatorCommerceWorkflow({ onComplete }: { onComplete?: () => void }) {
  return <PlaceholderPage title="Creator Commerce" description="Monetize your creations." />;
}