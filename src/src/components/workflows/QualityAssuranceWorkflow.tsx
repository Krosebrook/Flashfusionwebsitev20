import PlaceholderPage from '../ui/PlaceholderPage';
export default function QualityAssuranceWorkflow({ onComplete }: { onComplete?: () => void }) {
  return <PlaceholderPage title="Quality Assurance" description="Automated testing and QA." />;
}